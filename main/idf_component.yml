## IDF Component Manager Manifest File
dependencies:
  idf:
    version: ">=5.3.2"
  
  # --- Отключено: Требует IDF >= 5.5.2 ---
  # llgok/cpp_bus_driver:
  #   version: "^1.1.0"
  #   rules:
  #     - if: "target in ['esp32s3']"

  # --- Отключено: Требует IDF >= 5.4.0 ---
  # espressif/mcp-c-sdk:
  #   version: "~1.*"
  
  # --- Отключено: Требует IDF >= 5.4.0 (Видео) ---
  # espressif/esp_video:
  #   version: "==1.3.1"
  #   rules:
  #     - if: "target in ['esp32s3']"

  # --- Отключено: Требует IDF >= 5.4.0 (Дисплей CO5300) ---
  # espressif/esp_lcd_co5300:
  #   version: "^2.0.3"
  #   rules:
  #     - if: "target in ['esp32s3']"

  # --- Основные зависимости для XiaoClaw ---
  espressif/esp_lcd_ili9341:
    version: "^1.2.0"
  
  espressif/esp_lcd_gc9a01:
    version: "^1.1.0"

  espressif/esp_lcd_st77916:
    version: "^1.0.1"

  # LVGL графическая библиотека
  lvgl/lvgl:
    version: "^8.4.0"
    require: public

  # Компоненты для работы с дисплеем и сенсором
  espressif/esp_lcd_touch:
    version: "^1.1.0"
  
  espressif/esp_lcd_touch_gt911:
    version: "^1.0.0"

  # MQTT клиент (ИСПРАВЛЕНО ИМЯ)
  eclipse/paho-mqtt:
    version: "^1.0.0"

  # JSON парсер
  cJSON:
    version: "^1.0.2"

  # Утилиты
  espressif/esp_console:
    version: "^1.0.0"

# Настройки менеджера компонентов
manager:
  skip_checks: true
  ignore_warnings: true
